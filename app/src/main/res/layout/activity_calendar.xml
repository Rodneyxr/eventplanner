<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.CalendarActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#ece9d8">

        <include layout="@layout/toolbar_actionbar_with_headerbar" />

        <CalendarView
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:id="@+id/calendarView"
            android:showWeekNumber="false"
            android:selectedWeekBackgroundColor="@null"
            android:selectedDateVerticalBar="@color/bg_btn_join"
            android:focusedMonthDateColor="@color/session_photo_scrim"
            android:weekSeparatorLineColor="@color/data_item_lightbg_title"/>
    </LinearLayout>

    <!-- Nav drawer -->
    <include layout="@layout/navdrawer" />

</android.support.v4.widget.DrawerLayout>
